<div class="jumbotron card card-block">
    <% if @pending_audit_confirmations.any?  %>
       <h3>Your pending confirmation</h3>
       <%= render partial: "pending_audit_confirmation", collection: @pending_audit_confirmations %>
    <% else%>
       <h3>You don't have any pending confirmation.</h3>
    <% end %>
</div>
<div class="jumbotron card card-block">
    <%= link_to 'Request overtime approval', new_post_path, class: "btn btn-warning btn-block" %>
</div>

