<div>
    <h2><%= @post.user.full_name %> | <%= @post.rationale %></h2>
</div>

<div class="well">
    <div>
        <h3>overtime amount requested <%= @post.overtime_request %> h</h3>
    </div>

    <div>
        <h3>Date: <%= @post.date %></h3>
    </div>

    <div>
        <h3>Current stage of approval: <%= @post.status %></h3>
    </div>
</div>

<%= link_to 'Edit', edit_post_path(@post), class:"btn btn-primary btn-block" if policy(@post).update?%>